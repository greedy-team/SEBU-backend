# Generate a Base64 secret from at least 32 random bytes. See README.md.
JWT_SECRET_BASE64=
BACKEND_PORT=8080

# Optional monitoring: add "monitoring" to the active profiles and provide a separate random token.
# MONITORING_TOKEN=
